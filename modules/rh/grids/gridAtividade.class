<?
/* Copyright 2006, 2007, 2008, 2009, 2010 do SETEC/MEC
 *
 * Este arquivo é parte do programa SigaEPT
 *
 * O SigaEPT é um software livre;  você pode redistribuí-lo e/ou modificá-lo dentro dos
 * termos da Licença Pública Geral GNU como publicada pela fundação do software livre (FSF);
 * na versão 2 da Licença.
 *
 * Este programa é distribuído na esperança que possa ser útil, mas SEM NENHUMA GARANTIA; sem
 * uma garantia implícita de ADEQUAÇÃO a qualquer MERCADO ou APLICAÇÃO EM PARTICULAR. Veja Licença
 * Pública Geral GNU/GPL em português para maiores detalhes.
 *
 * Você deve ter recebido uma cópia da Licença Pública Geral GNU, sob o título “LICENCA.txt”,
 * junto com este programa, se não, acesse o portal do Software Público Brasileiro no endereço
 * www.softwarepublico.gov.br ou escreva para Fundação do Software Livre (FSF) Inc.,51 Franklin
 * St, Fifth Floor, Boston, MA 02110-1301, USA
 */
class gridAtividade extends MObjectGrid
{
	protected $objVinculo;
    /**
     * Constructor
     */
    function __construct($objVinculo)
    {
        global $MIOLO;
        $perms = $MIOLO->getPerms();

		$this->objVinculo = $objVinculo;
		
        $columns = array(
            new MObjectGridColumn('idAtividade','Número','left', false,'10%',false),
        	new MObjectGridColumn('atividade','Atividade','left', false,'70%',true),
            new MObjectGridColumn('dataInicio','Início','left', false,'15%',true),
            new MObjectGridColumn('dataFim','Fim','left', false,'15%',true),
            
        );

        $ctlArray = array (
            new FormButton('btnNew', 'Incluir Atividade',$MIOLO->GetActionURL('rh','main:vinculo',$objVinculo->idVinculo, array('form'=>'atividade','id'=>'new')))
        );

        
        $href_grid = $MIOLO->GetActionURL('rh','main:vinculo');
        parent::__construct($objVinculo->atividades, $columns, $href_grid,25);
        
        $this->SetTitle("Atividades");
        $this->setClose(NULL);
        $this->SetFilter(false);
        $this->SetIndex(1);
        $this->SetWidth('100%');
        $this->SetLinkType('hyperlink');
        if ( $objVinculo->isAtivo() && $perms->checkAccess('rh_atividade',A_ACCESS) )
        {
        	$action = $MIOLO->GetActionURL('rh','main:vinculo',$objVinculo->idVinculo, array('form'=>'atividade','id'=>'#0#'));
            $this->AddActionIcon('ID','select.gif',$action);
           	$this->SetControls($ctlArray);
        }
    }

    function GenerateHeader()
    {
        return NULL;
    }

    function GenerateFooter()
    {
        if (!$this->data) 
        $footer[] = $this->GenerateEmptyMsg();
        $footer[] = new Separator();
        $footer[] = $this->GenerateControls();
        return $footer;
    }
}
?>
