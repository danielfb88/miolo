<?
/* Copyright 2006, 2007, 2008, 2009, 2010 do SETEC/MEC
 *
 * Este arquivo é parte do programa SigaEPT
 *
 * O SigaEPT é um software livre;  você pode redistribuí-lo e/ou modificá-lo dentro dos
 * termos da Licença Pública Geral GNU como publicada pela fundação do software livre (FSF);
 * na versão 2 da Licença.
 *
 * Este programa é distribuído na esperança que possa ser útil, mas SEM NENHUMA GARANTIA; sem
 * uma garantia implícita de ADEQUAÇÃO a qualquer MERCADO ou APLICAÇÃO EM PARTICULAR. Veja Licença
 * Pública Geral GNU/GPL em português para maiores detalhes.
 *
 * Você deve ter recebido uma cópia da Licença Pública Geral GNU, sob o título “LICENCA.txt”,
 * junto com este programa, se não, acesse o portal do Software Público Brasileiro no endereço
 * www.softwarepublico.gov.br ou escreva para Fundação do Software Livre (FSF) Inc.,51 Franklin
 * St, Fifth Floor, Boston, MA 02110-1301, USA
 */

// alterar para persistencia
class BusinessRhParentesco extends MBusiness
{
	public $item1;  
	public $item2;
	public $item3;

    function __construct($data=null)
    {
       $this->Business('sigaept',$data);
    }

    function SetData($data)
   
    {
       $this->item1 = $data->item1;
       $this->item2 = $data->item2;
       $this->item3 = $data->item3;
    }

   function GetById($id)
	{
	 $sql = new sql('item2','cm_tabelageral'," ( (item2 = ?) ) and tabela='RH_PARENTESCO'");
		$query = $this->Query($sql,$item1);
		if ( !$query->eof() )
		{
			$this->SetData($query->GetRowObject());
		}
		return $this;
	}
	/*
    function Insert()
    {   global $MIOLO;
        $sql = new sql('idtipodepen, descricao','rh_tipodepen');
        $args = array(
    		$this->idtipodepen,
		    strtoupper($this->descricao)
        );
        $ok = $this->Execute($sql->Insert($args));
        if ($ok) {$this->Log(OP_INS,"idtipodepen = $this->idtipodepen");} 
        return $ok;
    }

    function Update()
    {
        $sql = new sql('descricao','rh_tipodepen','idtipodepen = ?');
        $args = array(
		    strtoupper($this->descricao),
    		$this->idtipodepen
        );
        $ok = $this->Execute($sql->Update($args));
        if ($ok) {$this->Log(OP_UPD,"idtipodepen = $this->idtipodepen");} 
        return $ok;
    }
    
    function Delete()
    {
        $sql = new sql('','rh_tipodepen', 'idtipodepen = ?');
        $ok = $this->Execute( $sql->Delete($this->idtipodepen) );
        if ($ok) {$this->Log(OP_DEL,"idtipodepen = $this->idtipodepen");} 
        return $ok;
    }
*/
	function ListRange($range=NULL)
    {
        $sql = new sql('item1, item2', 'cm_tabelageral', "tabela='RH_PARENTESCO'",'item2');
        $sql->SetRange($range); 
        $query = $this->Query($sql);
        return $query;
    }

	function ListAll()
    {
		return $this->ListRange();
    }

}
?>
                           
