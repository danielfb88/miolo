<?
/* Copyright 2006, 2007, 2008, 2009, 2010 do SETEC/MEC
 *
 * Este arquivo é parte do programa SigaEPT
 *
 * O SigaEPT é um software livre;  você pode redistribuí-lo e/ou modificá-lo dentro dos
 * termos da Licença Pública Geral GNU como publicada pela fundação do software livre (FSF);
 * na versão 2 da Licença.
 *
 * Este programa é distribuído na esperança que possa ser útil, mas SEM NENHUMA GARANTIA; sem
 * uma garantia implícita de ADEQUAÇÃO a qualquer MERCADO ou APLICAÇÃO EM PARTICULAR. Veja Licença
 * Pública Geral GNU/GPL em português para maiores detalhes.
 *
 * Você deve ter recebido uma cópia da Licença Pública Geral GNU, sob o título “LICENCA.txt”,
 * junto com este programa, se não, acesse o portal do Software Público Brasileiro no endereço
 * www.softwarepublico.gov.br ou escreva para Fundação do Software Livre (FSF) Inc.,51 Franklin
 * St, Fifth Floor, Boston, MA 02110-1301, USA
 */

/**
 * Formulário de edição da Ocorrência do Grupo Ocorrência
 */
class frmOcorrenciaDados extends Form
{
	/**
	 * Objeto ocorrencia
	 * @access protected
	 * @var BusinessRhOcorrencia
	 */
	protected $objocorrencia;

    function __construct($objocorrencia)
    {
	$this->objocorrencia = $objocorrencia;
        parent::__construct('Editar Dados da Ocor
	rência');
        $this->EventHandler();
    }

    function CreateFields()
    {
       global $MIOLO;

        $situacao = $MIOLO->GetBusiness('rh','situacaofuncional');
		
   	   $fields = array(
           new TextField('descricao','','Descrição','60'),
           new Selection('idsituacao','','Situação')
        );
		
        $this->SetFields($fields);

	$query = $jornadas->ListAll();
        $this->SetFieldAttr('idsituacao','options',$query->chunkResult());
		
		$buttons = array(
			new FormButton('btnSalvar','Alterar')
            );
		$this->SetButtons($buttons);
		$this->SetData();
    }

	function SetData()
	{
		$data = $this->objocorrencia;
		$this->SetFieldValue('descricao',$data->descricao);
		$this->SetFieldValue('idsituacao',$data->idSituacao);

	
	}

	public function btnSalvar_click()
	{
		$this->objocorrencia->SetData($this->GetData());
		$this->objocorrencia->save();
	}

 }
?>
